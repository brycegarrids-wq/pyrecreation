# pyrecreation
Basically, python in python.
# Printing
Use the pr class to print, pr.anypr() prints any type.
Every type has its own print() like, pr.setpr(), pr.complexpr(), except range, bytes, bytearray, memoryview and None
